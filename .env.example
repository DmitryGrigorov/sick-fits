# Used by docker-compose.yml to configure the local PostgreSQL container.
# These must match backend/.env's DATABASE_URL.
POSTGRES_USER=sickfits
POSTGRES_PASSWORD=sickfits
POSTGRES_DB=sickfits
POSTGRES_PORT=5432
